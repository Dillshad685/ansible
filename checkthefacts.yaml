- name: check the facts
  hosts: all
  connection: local
  become: yes
  tasks:
  - name: install nginx if RedHat
    ansible.builtin.dnf:
     name: nginx
     state: latest
    when: ansible_os_family == "RedHat"

